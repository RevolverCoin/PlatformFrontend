import { connect } from 'react-redux'

import UserPostsItem from '../components/UserPostsItem'



const mapStateToProps = state => ({
})

const mapDispatchToProps = dispatch => ({
})

export default connect(mapStateToProps, mapDispatchToProps)(UserPostsItem)
