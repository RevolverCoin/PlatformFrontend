import { connect } from 'react-redux'

import UserTopListItem from '../components/UserTopListItem'

const mapStateToProps = state => {

  return {
  }

}

const mapDispatchToProps = dispatch => ({
})

export default connect(mapStateToProps, mapDispatchToProps)(UserTopListItem)
